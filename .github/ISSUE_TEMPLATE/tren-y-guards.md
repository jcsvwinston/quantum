---
name: Tren de releases o guard
about: Un script de scripts/train/ que se para donde no debe, un guard que muerde de más (o de menos), una fixture que no representa la rotura.
title: "tren: "
labels: tren
---

**Script o guard** (ruta): `scripts/…`

**Comando exacto y salida**:

```
```

**Estado del árbol** (`git submodule status`, `git status --porcelain`, rama):

**Qué debería haber pasado**:

Si es un guard que muerde de menos, di qué rotura debería cazar: la fixture
en `tests/guard-fixtures/<guard>/` es el sitio donde esa rotura se demuestra.
